import AdminApp from '@/components/admin/AdminApp'; import { notFound } from 'next/navigation'; const valid=['news','news-categories','articles','article-categories','blogs','blog-categories','careers','applications','enquiries','users']; export default async function Page({params}:{params:Promise<{resource:string}>}){const {resource}=await params;if(!valid.includes(resource))notFound();return <AdminApp resource={resource}/>}
